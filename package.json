{
  "name": "rainbot-sands",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --recursive dev",
    "test": "pnpm --recursive test",
    "format": "oxfmt",
    "format:fix": "oxfmt --write",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix"
  },
  "devDependencies": {
    "oxfmt": "^0.55.0",
    "oxlint": "^1.70.0"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.2.1",
      "onFail": "download"
    }
  }
}
